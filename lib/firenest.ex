defmodule Firenest do
  @moduledoc """
  Firenest is a framework for building distributed applications.

  It contains the following components:

    * `Firenest.Topology` - the core of Firenest which handles node
      discovery, node failure, broadcast and message passing between nodes

    * `Firenest.PubSub` - a distributed and scalable PubSub implementation

  """
end
